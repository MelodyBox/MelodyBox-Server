export class _YTMusic {
  constructor() {}
  // temp
  protected pythonCall(...args: any[]): { success: true; data: any } | { success: false; error: string } {
    return { success: false, error: "Error" };
  }
}
